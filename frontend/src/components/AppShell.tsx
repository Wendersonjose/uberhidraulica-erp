import{LogoutButton}from'../auth/LogoutButton';import{NavLink,Outlet}from'react-router-dom';import{useAuth}from'../auth/useAuth';const links=[['Clientes','/clientes'],['Veículos','/veiculos'],['Ordens de serviço','/ordens-servico'],['Serviços','/servicos']] as const;export function AppShell(){const{session}=useAuth();return <div className="app-layout"><aside className="sidebar"><div className="brand">UBERHIDRÁULICA<small>Gestão de Oficina</small></div><nav className="nav" aria-label="Principal"><span>Visão geral</span>{links.map(([label,to])=><NavLink key={to} to={to}>{label}</NavLink>)}<span>Estoque</span><span>Financeiro</span><span>Configurações</span></nav><LogoutButton className="logout">Sair · {session?.name}</LogoutButton></aside><main className="content"><Outlet/></main></div>}
